README.md
CLAUDE.md
CHANGELOG.md
.cache
.password_manager.sh
*.code-workspace
docs/
.worktrees

# ZShell tmp file
*.zwc
*/*.zwc
*/.zcompdump
.config/zsh/.zcompdump
.config/zsh/.zcompcache
.config/zsh/.zsh_history

# chezmoi
# `~/.config/chezmoi/chezmoi.toml` は `.chezmoi.toml.tmpl`（ソースルート）から
# `chezmoi init` で生成されるローカル設定。`dot_config/chezmoi/` 配下からは
# deploy しない（promptStringOnce 等が使えないため）。
.config/chezmoi/*

.config/gh/private*.yml
.config/gh/hosts.yml

# Rye
.local/share/rye/*
!.local/share/rye/config.toml

# GNUPG
.local/share/gnupg/*
!.local/share/gnupg/common.conf

{{ if eq .chezmoi.os "windows" }}
# Windows
  AppData/Roaming/nushell/history.txt
{{ else }}
# Not Windows
  AppData
{{ end }}
{{ if ne .chezmoi.os "linux" }}
  .config/app/file.conf
{{ end }}

# tmux-resurrect / continuum 自動保存（生成物 — chezmoi 管理対象外）
.local/share/tmux/*
# claude-tools Go ソース (バイナリは chezmoi run_onchange で build → ~/.local/bin/)
programs/

# Containerized test environment — never deploy to ~/
test/
.github/workflows/container-smoke.yml
