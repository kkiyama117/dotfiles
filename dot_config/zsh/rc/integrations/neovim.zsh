if (( $+commands[nvim] )) then
  alias vim=nvim
  alias vi=nvim
  alias edit=nvim
  alias edit=nvim
  alias dedit=dvi
  # debugging nvim
  alias dvi="DEBUG=true nvim"
  alias dedit=dvi
  alias vimo=/usr/bin/vim
  alias vimdiff="nvim -d"
fi
